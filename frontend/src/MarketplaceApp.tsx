/**
 * Marketplace App Component
 * 
 * Standalone application for the Administration Marketplace.
 * This component can be opened in a new tab.
 */
import React from 'react';
import Marketplace from './components/Marketplace';

const MarketplaceApp: React.FC = () => {
  return <Marketplace />;
};

export default MarketplaceApp;


