# NPM Configuration for Enterprise Environment
# This file prevents npm from trying to fetch blocked packages

# Optional: Configure registry if needed
# registry=https://registry.npmjs.org/

# Ignore scripts that might fail (we handle fast-equals replacement manually)
# ignore-scripts=false

