# ETL Pipelines - Standalone Scripts Requirements
# These scripts can run independently without the main backend application

# Core Dependencies (Required)
# ============================

# TreeSitter for multi-language code parsing
tree-sitter>=0.20.4
tree-sitter-python>=0.20.4
tree-sitter-java>=0.20.2
tree-sitter-javascript>=0.20.1
tree-sitter-go>=0.19.1
tree-sitter-rust>=0.20.3

# NetworkX for graph operations
networkx>=3.0

# Optional Dependencies (For Advanced Features)
# ==============================================

# OpenAI for generating embeddings (optional - only if you want embeddings)
openai>=2.0.0

# Azure OpenAI Embeddings (optional - alternative to OpenAI)
# azure-identity>=1.15.0  # For certificate-based authentication
# langchain-openai>=0.1.0  # For AzureOpenAIEmbeddings

# TigerGraph/TigerDB (optional - for graph database)
# pyTigerGraph>=1.2.0  # For porting NetworkX graphs to TigerDB

# OpenSearch client (optional - only if you want to index/query OpenSearch)
opensearch-py==2.4.0

# AWS Authentication for OpenSearch (optional - only if using AWS OpenSearch)
# aws-requests-auth>=0.4.3  # For AWS authentication
# boto3>=1.34.0  # AWS SDK for Python

# Note: All scripts in this directory are completely self-contained and independent.
# No backend dependencies are required.

# Advanced ETL Features (Phase 1 & 2 - Recommended)
# ==================================================

# Progress Tracking & UX
tqdm>=4.66.0  # Progress bars for long-running operations

# Parallel Processing
joblib>=1.3.0  # Parallel file processing (speeds up large repos significantly)

# Data Validation
pydantic>=2.0.0  # Schema validation for chunks

# Data Analysis & Statistics
pandas>=2.0.0  # Analyze chunk statistics and data quality metrics

# Optional Advanced Features (Phase 3)
# =====================================
# Uncomment these for additional features:

# Rich terminal output
# rich>=13.0.0  # Rich terminal output with tables and formatting

# Efficient Data Storage
# pyarrow>=14.0.0  # Columnar storage format (faster for large datasets)

# Workflow Orchestration (Advanced)
# prefect>=2.14.0  # Orchestrate complex ETL pipelines with retries and scheduling

